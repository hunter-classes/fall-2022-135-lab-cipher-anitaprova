#pragma once 
#include <vector>
#include <string>

int capital(char c);
std::vector<double> file_freq();
std::vector<double> freq(std::string str);
std::vector<double> shift(std::string str, int shift);
double distance(int x1, int y1, int x2, int y2);
std::string solve(std::string encrypted_string);
